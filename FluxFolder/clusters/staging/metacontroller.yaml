apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: metacontroller
  namespace: config-scmvanilla-z6w8eq
spec:
  interval: 10m0s
  dependsOn:
    - name: infrastructure
  sourceRef:
    kind: GitRepository
    name: config-scmvanilla-z6w8eq
    namespace: config-scmvanilla-z6w8eq
  path: ./FluxFolder/metacontroller
  prune: true
  healthChecks:
    - apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      name: metacontroller
      namespace: config-scmvanilla-z6w8eq